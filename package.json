{
  "name": "jessestricker.github.io",
  "license": "MIT",
  "source": "src/index.html",
  "browserslist": "defaults",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --log-level=verbose --no-source-maps --no-autoinstall",
    "clean": "rm --recursive --force ./.parcel-cache/ ./dist/",
    "lint": "stylelint 'src/**/*.scss'",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "prettier": "^3.3.2",
    "stylelint": "^16.3.1",
    "stylelint-config-recess-order": "^5.0.0",
    "stylelint-config-standard-scss": "^13.1.0"
  },
  "dependencies": {
    "@fontsource-variable/source-sans-3": "^5.0.20"
  }
}
